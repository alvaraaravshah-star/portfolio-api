services:
  - type: web
    name: macro-engine-api
    env: python
    plan: free
    buildCommand: "pip install --only-binary :all: -r requirements.txt"
    startCommand: "uvicorn api_server:app --host 0.0.0.0"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
